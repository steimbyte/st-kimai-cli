/**
 * Application constants
 */

/** Default API request timeout in milliseconds */
export const DEFAULT_TIMEOUT_MS = 30000;

/** Table formatting width */
export const TABLE_WIDTH = 70;

/** Label formatting width */
export const LABEL_WIDTH = 14;

/** Minimum gap between entries in minutes to be considered a break */
export const MIN_GAP_MINUTES = 5;

/** Default break duration in minutes */
export const DEFAULT_BREAK_MINUTES = 30;
